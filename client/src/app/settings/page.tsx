"use client";

import React from "react";

type UserSettings = {
   label: string;
   value: string | boolean;
};

const Settings = () => {
   return <div>Settings</div>;
};

export default Settings;
